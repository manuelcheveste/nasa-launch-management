{
  "name": "nasa-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-deps:server": "npm install --prefix server",
    "install-deps:client": "npm install --prefix client",
    "install-deps": "npm install && run-p install-deps:*",
    "dev:server": "npm run watch --prefix server",
    "dev:client": "npm start --prefix client",
    "dev": "run-p dev:*",
    "deploy": "npm run build --prefix client && npm start --prefix server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
